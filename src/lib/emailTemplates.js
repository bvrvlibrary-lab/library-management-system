export function issueBookTemplate(
  request,
  issueDate,
  dueDate
) {
  return `...`;
}

export function renewalTemplate(
  request,
  newDueDate
) {
  return `...`;
}

export function returnTemplate(
  request
) {
  return `...`;
}
